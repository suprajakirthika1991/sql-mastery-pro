// Seed data for the in-browser SQLite playground (identical to the prototype SEED object).
export const SEED: Record<string, Record<string, unknown>[]> = {
  departments: [
    { id: 1, name: "Engineering", location: "Sydney" },
    // … port full dataset from prototype
  ],
};
